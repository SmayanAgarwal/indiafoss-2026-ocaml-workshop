[
  {
    "slide": "slides/scene_0001.png",
    "t_start": 0.0,
    "t_end": 238.52714285714288,
    "text": "Okay, so you should see the presentation. Okay, good. So the last thing that I wanted to cover in this lecture is using memoization, which we saw in the last class for doing something a little bit more real, right? In particular, we are going to apply this on a well-known algorithm. And the point that I want to sort of try to push is the fact that memoization is a general solution for dynamic programming. So the problem that you are going to look at is edit distance. It is also known as Levenshtein distance. The idea is that in this problem, you will be given two strings. And the question that we are asking is, what is the cost of editing the first string in order to change it into the second string, right? And what do you mean by editing? The three operations that are allowed are insert, delete, and replace of characters, right? And we say that cost of each one is one each, right? So this is a very standard problem, right? Levenshtein distance is a well-known problem. If you've done algorithms, you would have come across this problem previously, right? And the standard way of solving this is dynamic programming, right? So you would use dynamic programming to achieve an efficient solution. So the observation that we are going to sort of share in this lecture is that there is a nice algorithmic way of describing the problem, right? Which does not particularly care about efficiency. So and what we'll see is that we will get efficiency for free through the use of memoization, right? So we will take the solution. We'll have to write some solution down. We'll write a solution. On the first look, it will be inefficient, and we will see that it is inefficient. And then what we will do is we will get efficiency for free through memoization.  So just to remind you of what the Levenshtein distance problem is. So here is edit distance between kitten and sitting, right? So it takes two strings. I'm asking, okay, what is the cost of editing kitten to change it to sitting? The solution is three, right? And of course, we are looking for the minimal cost, right? So edit distance is the minimal cost for going from the string one to string two. So you first change K to S, right? In kitten, you change K to S, replace the character to get sit in. You change the E to I, right? And finally, insert a new character. You get sit in here. So insert a new character at the end, which will change it to sitting. So since there are three operations, the cost is three. Okay, so that's the problem through an example. And of course, the idea is that the strings can be of different lengths, right? So it is of different lengths here. So how do we solve this problem? So here is the standard solution. Okay, so there is a lot going on here. I will try to break it down, but it is not important that you understand the correctness of the algorithm here. What I want you to focus on is the implementation of the algorithm, right? The structure of the algorithm."
  },
  {
    "slide": "slides/scene_0002.png",
    "t_start": 238.52714285714288,
    "t_end": 477.05428571428575,
    "text": "So let's break it down bit by bit. So edit distance is a function, right? It's a recursive function. There is an optional argument log. We'll ignore that for the moment. That's just turning off on and off printing. It takes two strings, right? It takes an S and a T. And the question is, what is the cost for going from S to T using insert, replace or delete? Spring operations here. So the thing that we match with here is the length of strings of S and T. Okay, so the first thing that we do is we pattern match on these two patterns. So the pattern says that if either of these strings are actually empty, the string length is zero, then the edit distance is the length of the other string. Right, so because if one of the strings is zero, then you can get from one to the other by deleting the characters one by one or inserting the characters one by one. Right, that's easy. So if the strings are of non-zero lengths, both of the strings are of non-zero lengths. What we are going to do is to get the substrings of S and T, S prime and T prime. The substring is constructed such that it just does not have the last character. We just drop the last character. Right, so we are essentially breaking down the problem into smaller problem. And the way we actually compute the edit distance is there are going to be three possibilities. Right, and we are going to get the minimum of these three possibilities because edit distance is the minimum distance to go from one to the other. Right, what are the three possibilities? The three possibilities are compute the edit distance to go from S prime to T. S prime is S without the last character. Right, compute the edit distance for going from S prime to T. And if the cost of that is some C, then the cost of going from S to T is that C plus one. What is this additional one? The additional one is there in order to delete the character of S. Right, in order to go from S to S prime, you need to delete it. So that is why we have plus one here. And similarly, we also explore the other possibility, which is compute the cost of going from S to T prime. Right, where you and if the cost is T, then the cost of going from S to T is that plus one. And, yeah, OK, sorry. So the cost of that is inserting the last character to T prime. Right. So, OK. So because T prime is obtained by deleting that character. So if you can go from S to T prime, then you need to insert the last character to go to T and that is the cost for one. And the third case is compute the edit distance between S prime and T prime. Right. And if the last characters of S and T are the same, then you don't need to do anything. Right. So no change, no cost for if the last characters are both the same. In case the last characters are different, then you add cost one because you need to change the last character of S to T and the cost for that is one. Right. So, OK. So this is the algorithm. So I'm not going to explain how this algorithm works. But what I want you to focus on is that there are three recursive calls. Right. One, two and three. Right. So naively, if you just run this algorithm, what will happen is you will have several invocations of the same function with the same argument."
  },
  {
    "slide": "slides/scene_0003.png",
    "t_start": 477.05428571428575,
    "t_end": 715.5814285714285,
    "text": "You will see how this appears. What we are going to aim to do is to make sure that these repeated invocations are all memoist in the sense that if you if you've seen those particular arguments earlier, we will say, OK, we'll just retrieve the result that we've previously done. And we'll see how that helps. The other thing that we do here is if the log is true, then we print S and T. This is just for debugging. Right. So it prints S equals that string S equals that string T. OK. So with that, I will run this program just to show how this works. So I'm using the BIMET function from earlier. So I'm going to compute the edit distance between OCaml and OCaml. So the difference is we are replacing these are capital letters. These are small. Right. So the edit distance is two. Right. And it took three milliseconds to compute this. Right. I've turned off log. Let me turn on the log. There is a lot of recursive calls. Right. I mean, there is a lot still going on. The thing that I want you to notice is there is a repeated invocation. So there is this. Oh, there is an empty string. There's one empty string here. I think there will be some one empty string again. Right. There are multiple invocations of one interesting over and over again. So this is taking a long time. Right. That is why this function is now taking a long time. Actually, this function completed. Let's put it back to false. This function completed in what? Two milliseconds with this string. Right. Two milliseconds doesn't sound bad. But this is the cost is really bad. The running time is really bad for longer strings. In particular, if I just change the string such that I add the same suffix four point to eight four point to eight. Right. And the only difference is OC here. The same OC here. Now look at the running time. It's taking a long, long time because the running time just explodes because of all of the recursive calls. It's still going on. Let's wait for a second. It's still going on. OK. That took 14 seconds. Right. So and the edit distance is still do so. This obviously is not a great algorithm. Right. You want to apply this algorithm on very long strings. Right. So I'm going to take two genetic sequences and I'm going to find how similar those genetic sequences are. If you sort of have a so these are going to contain those four characters. Right. And then you are going to see these very, very, very long strings. And you are going to ask what is the edit distance between the two so that you find the common similar looking genes using this algorithm. Of course, this algorithm as it is written down is not going to work because it is so inefficient. How can we make this efficient? Let's use memoization. Right. The only difference between the previous implementation and the implementation that is described here is to make it suitable for memoization. Right. So all I've done is I've added an extra argument for the recursive call F and I replace the recursive invocations with F here. Right. So this is an odd recursive function. I shouldn't need our recursive annotation here, but otherwise the function is the same. OK, so now let's memoize it using the memo recursive memo function that we've seen in the last class. So I'm minimizing that implementation now. And if I run this program now, it runs in point six two milliseconds."
  },
  {
    "slide": "slides/scene_0004.png",
    "t_start": 715.5814285714285,
    "t_end": 954.1085714285715,
    "text": "So earlier it took fourteen milliseconds. And the other thing that I want to show here is if you actually turn on the log, you will see that we still do a lot of comparisons. But take my word for it. None of there are no two similar comparisons. Right. So we won't ever see the same comparison again because we are we're actually minimizing everything. So once you actually have the previous result, we are using it. So the point to take away here. Right. So what we've done is we have a dynamic programming implementation which is almost comes off free from the direct inefficient implementation. Right. You have an algorithmic description of a well-known algorithm. And if you actually want to make it run fast, you'll have to do something. And because of everything being functional here, everything is purely functional. We are not manipulating any state except for the implicit state manipulation that occurs within the memoization. We are not doing anything else, but still we get very good performance. So the high level point to take away. Right. So is that imagine if you had to write this in C, the same algorithm in C and get a similar performance. If you had to do it by hand without having this memory functions and so on. Of course, you can make it go faster than what I've done here. Right. So I've done a very naive one with caches that keep on building and so on. You can, of course, make it maybe go 10 times faster. But the question you need to ask is C gives a lot of performance all the time. Right. But what do you lose for the performance you get? You lose type safety, right? It's weakly typed. It is also the case that the cost of actually arriving at the correct solution is much more difficult. Right. So, of course, you want to think about implementing critical parts, performance-sensitive parts in C. But most of the code that is there, even in things like operating systems are not performance-sensitive. So it is not always necessary that you want to see all the time. So in summary, the idea is that laziness and memorization makes sense only in a pure setting. Right. So because if your program has side effects and if you depend on those side effects to be required for correctness, of course, we have side effects in this memo. I say an example that we saw earlier. We just printed the log. And of course, we only printed the log once because the effect was minimized. But if you are the function specification requires side effects, say sending messages on on the network or just doing manipulating data structures in a side affecting way, you cannot apply memorization or laziness. So the thing to note is OCaml doesn't prevent you from performing side effects in your program. You can throw references anywhere, arrays anywhere. We are not going to get any guarantees from the compiler itself that it doesn't do any side effects. In that way, it is the same as C, C++, Java, Python, Rust, Swift, JavaScript, all of these languages. Right. You can have side effects anywhere, but partitioning your program into parts that are using side effects and ones which don't is useful. Right. For reasoning, but also for using things like memorization and laziness. Of course, there are languages like Haskell which tracks side effects in the type system."
  },
  {
    "slide": "slides/scene_0005.png",
    "t_start": 954.1085714285715,
    "t_end": 1192.6357142857144,
    "text": "So when you write Haskell programs, you can use mutable features. All of them are sort of compartmentalized in one way and they are kept separate from the pure parts of the code. And the high level takeaway is this. Right. Purity lets you take advantage of nicer reasoning, but also these sort of features. Make your code pure. If your performance is not critical, right. So I say that with a piece of salt, it doesn't mean that your code needs to be inefficient, but don't think about using mutable features in order to get maximum performance. Before you even have a solution for whether something is performance critical or not. That's the ideal idea. And if you keep something to be pure, you get other benefits. So that's the summary. So that is the lecture on streams, laziness and memorization. So any questions on the material that we've covered in this lecture so far? While I set up the next. I'll give a minute. You can think about. Do you have any questions? So. Yep. So like I had a doubt regarding like, let's suppose we have a doubly linked list. And in that, like if I have any node, so like camel is like a functional language. So like there'll be a single data structure in the memory to which we are pointing right now. So. Yeah. So when we are accessing like, let's suppose the previous or the next of this node. Yeah. So like, like, how do we know that this is exactly the previous or next of the this current node or are we creating a copy of this thing? So. So you have a node, right? That has previous and next. Yes, sir. Right. And what do you want to know? So like, let's suppose, let's take a list. For example, let's say I have a list of one, two, three, let's suppose. Okay. And if I'm at one and if I am, if I use the pattern matching to like deconstruct it into one is the head and two and three is another list. So like when I'm trying to access this two and three in the actual memory, like, is it kind of an alias for the actual two and three or like, do I have another data structure which is having the same value? Typically, it is alias, right? So, but it doesn't matter. Why does it matter? Because it's you cannot mutate that list. Yes, in the case of linked list, if I have mutable things. So in that matters, right? If I'm the next. Yeah, yeah, there it is not a new list. So it doesn't, it doesn't copy. So if you have a list, and then you say, get me the tail of the list, right, you write a function which returns the head of the list and the tail of the list. And the thing that you is the alias to the identity. So you don't you don't create new lists, you'll only create new lists when you actually use constructors to construct a new list. So so like, so when you say so if I have any function and I'm passing every like all the arguments will be passed by reference, right? Yeah."
  },
  {
    "slide": "slides/scene_0006.png",
    "t_start": 1192.6357142857144,
    "t_end": 1431.162857142857,
    "text": "Reference. Yeah, everything is reference. Yeah. Yeah, good question. So everything is by reference. You don't need to worry about the differences. Yeah, everything is. Okay, so there's a question on chat. Why isn't the type of lazy stream not recursive? Asin, why don't we use that keyword? Let me get that up. So lazy values. So why is this not recursive? You have a question. So these are the this is recursive, right? This type stream is the same type as the one that I'm defining here. We don't use a record for the type, right? So types are always considered to be recursive. For values. Yes. So let's look at the value. So these are just functions. So if I want to construct, yeah, so if I'm constructing a value, which is a lazy stream, right here, I use the record, because the thing that I'm defining here, the lazy stream is the one that I'm using here. Right. So for values, you have to use the keyword for types. They're always. Yeah, so for types, they are, they're always recursive. So whenever you say type, OCaml implicitly gives you the recursive type. So, okay, so that's that was easy. So anything else? Any other questions? I've been posting a few few tips and things to work with the assignments on the Slack channel, right? If you've not been following it, do have a look. So these are posted in the general channel. This is based on the questions you're asking, right? So I'm sort of like getting principles out of it and sort of giving you tips to debug your program. Ask me more questions, you'll get more tips like this. It's hard to present all of this without some context. So do have a look at the Slack channel. Ask me questions and I'm sure you can also ask the TA's questions, right? So they will also be able to help. You can DM me if you are asking questions that are specific to your code in the assignment. It is better to send me a direct message or to the TA's, right? So I usually respond. Anyway, anything else? Okay, good. So in that case, let me move on to the next lecture. So in this lecture, we are going to look at what are known as monads, right? This is just a word. I'll explain what the idea is. Essentially, monads are a way of dealing with effects in a pure setting. So as we call the conclusion that I mentioned in the last class, right? So OCaml is a language, it's a functional language."
  },
  {
    "slide": "slides/scene_0007.png",
    "t_start": 1431.162857142857,
    "t_end": 1669.69,
    "text": "It gives you all the nice features of writing pure programs, but you can also mix and match things that have effects within them. So you can throw references in it. You can do whatever you want, right? So from a study of functional programming languages, right? When you sort of think about Lambda calculus and so on, sometimes it is better to We had nice properties for purely functional parts of the language, right? We had this nice HurryHoud correspondence, which said, oh, every type is like a proposition and every program is like a proof of the proposition. All of that sort of fails Really badly. If you add ML like references, references do not drag into simply take Lambda calculus. Monads are this nice way of like reasoning about effects in a pure setting, right? So there are many, many things, but they are just a nice way to reason about these side effects in a pure setting. So if you wanted just a pure subject of a language and you wanted to add effects to the language and still retain some notion of program reasoning. So monad is a nice way to do it. So that is what we are going to look at. Actually, monad is also a design pattern. So it sort of helps you organize your program in a nice way. We'll sort of motivate and look at why it is a case. So the term monad is really weird, right? So it is not. It doesn't seem off this world is out of this world. Where does this come from? This comes from category theory and category theory is the study of mathematical abstractions. So category theorists like to give names for everything and they like to sort of give names, which are completely made up and different from everything else so that they can name things. Which are quite abstract, right? And monads is sort of the term monad is borrowed the idea and the term monad is borrowed from category theory. Category theory itself is out of scope of this course. I'm not going to even touch up on that. But what we'll see is what is the programming aspect of monads? So there was a really nice paper back in 91 by this person called Phil Wadler. I'll share a link to the paper later who said there's this nice idea in category theory where mathematicians are looking at just mathematical abstractions. And he said, OK, if we take this idea and put it into an actual programming language, it makes some of the problems that we have easier. And that is how monads came to be in programming languages. And what we will do in this lecture is we'll just look at programming with monads. We are not going to look at. We're going to touch upon some principles, but not look at it in any detail. OK, so yeah, as I said, so this person borrowed Phil Wadler borrowed the idea of monads from category theory. Right. And then he he was one of the key persons in the design of Haskell programming language. And Haskell is a purely functional programming language in the sense that in OCaml we were able to throw side effects, arrays and references wherever we wanted in the program. And it's sort of worked out in Haskell. We are not able to do that because the type system will say you cannot have any side effects in your purely functional code."
  },
  {
    "slide": "slides/scene_0008.png",
    "t_start": 1669.69,
    "t_end": 1908.217142857143,
    "text": "So they are just completely encapsulated in some way when the Haskell function is pure. When it says int arrow int, it has a contract that this particular function will not perform any side effects. This is very nice because just by looking at the type, you can know something about the function. And in particular, these functions behave like mathematical functions. It's what we saw in the earlier part of the course. Right. And we broke all of this when we added side effects. But that's another story. But there was this question of how do you integrate side effects into a language like Haskell, which separates, which sort of encourages pure code all the time. And monads are a way to do it. You might ask why you would want monads in say OCaml, right, where we can have side effects everywhere in the language. One thing monads allow you to do is they allow you to simulate and encapsulate side effects. In particular, there is a nice result which says that any effect that you want to simulate by effects, you can mean say references, state, say throwing exceptions, concurrency, parallelism and so on. All of these would be simulated by monads in the sense that you can implement them as libraries. So if you wanted a concurrency library, if you wanted, say, high performance asynchronous programming library in OCaml, I will write the library using a monad just so that it eliminates the presence of concurrency, which OCaml does not have. OCaml does not have native concurrency features. So the idea is that you can simulate advanced language features just by using a monad. And it might seem like monads are this intellectual curiosity that is only explored by these languages which are not widely used Haskell and OCaml. But these sort of ideas are slowly coming into mainstream languages as well. Scala, for example, which is heavily used by Twitter, right? So they use monads everywhere. And there are libraries which use monads. And of course, these ideas are also being borrowed into other languages. I've seen C++ libraries which use monadic programming patterns and so on. So the aim of this particular lecture is to give you an idea of what a monad is in a nice setting, right? So we are going to see simple examples and so on so that you can you can see applications of it elsewhere. When you see it in, say, a JavaScript library or when you see it in, say, Rust library, right? When you actually end up using one of those languages, you can identify the programming patterns and hopefully understand the semantics of what they do without delving into the implementation details, right? We are going to look at what they are more than how they are implemented. So that's the idea of this lecture. And as I mentioned before, monad is a design pattern, right? It's an EDM. It is not a primitive language feature. So just like you have design patterns in other languages, we can also have design patterns in OCaml. And monad is just a design pattern. It's a way of organizing the program in such a way that it works for you, right? But it's not like a language feature that is embedded into the language. We are going to use all the language features that we've used previously. In all of the previous lectures, we are going to borrow them. We're going to use it in this lecture. So, okay, so the question, the first question is, what is a monad, right? What exactly is a monad? The answer is actually very simple, but not useful. But I'll anyway give you the answer. And then we will look at examples to motivate why this is a useful answer."
  },
  {
    "slide": "slides/scene_0009.png",
    "t_start": 1908.217142857143,
    "t_end": 2146.744285714286,
    "text": "So a monad is any implementation that satisfies the following signature, right? So we have a module signature here, right? It's a module type monad, right? And it has three things, right? It has a type, it has two functions return and bind. So any structure that satisfies the signature is a monad. And in addition, it has to satisfy what are known as monad loss, we will see what those monad loss are. But if you can write a structure that satisfies the signature and the monad loss, then you have a monad. So this answer is a very abstract one. It's not illustrative, right? It's not giving you any more information than when you started with, when you asked this question. Let me try to refine it a little bit. So a monad, right, is meant to describe a computation. Computation would be anything, let's leave it abstract. So the type of that computation is alpha t, right? There is some computation, something that computes. And the type of that computation is alpha t, where the alpha corresponds to the return value of the computation, right? When you run the computation, that's the result of the computation, right? So computation is this thing, which I haven't defined. But when you evaluate that, run that computation, you get an alpha as a result. And return is a function that takes some value alpha and then makes it into a computation. And the idea is that the computation will have some effects. But I also want to take values and make it into a computation such that it doesn't perform any interesting effects. But when this computation is run, you get the value back, this value back, right? That's return. And bind is a way of sequencing to computations, right? Ignore the signature for now. Bind is just binding to computations together to create a bigger computation. So take two computations, it's like a glorified semicolon in OCaml, right? In OCaml, you can take one print, another print, and then add a semicolon. You have an expression now, which prints both of the things. Just like that, bind is like a glorified semicolon, which takes a computation alpha t, right? And it takes a function that takes the result of this computation, which itself returns a computation, which is beta t. So there are two computation, one computation is the alpha t computation, another one is the beta t computation. We're going to fuse them together, right? So at the end, you're going to get beta t. So you build a new computation, right? Which is going to return you a beta t. That's the abstract description, right? So it's sort of keep that in the back of the mind. It's okay if all of this is not clear. The idea is monads, disk array computations, return values, lift a value to a computation, and bind takes two computations and sequences them together. That is all you need to know at this point. Okay, this all seems very abstract, right? And of course, many functional programming concepts are. And in order to make it more clear, we will take a simple example that we understand. And then we will slowly motivate why we need monads, right? And that's what we will do in this lecture, hopefully."
  },
  {
    "slide": "slides/scene_0010.png",
    "t_start": 2146.744285714286,
    "t_end": 2385.2714285714287,
    "text": "So what we're going to do is to write a very simple interpreter. They're going to write a very simple interpreter for arithmetic expressions. Okay. So here is the expression language that I have. So we define a type for expressions. The expression could be one of three things. It could be a value. It could be an integer value, right? It's a value of integer. So values are expressions. It could be the addition of two expressions, right? Plus expression and expression two. Or it could be expression one divided by expression two. So there are only three kinds of expressions. And of course, this can be recursive, right? So these are expressions themselves. So a value, addition or division, right? So we want to take the expressions that are defined using this. And we are going to write an interpreter that evaluates that to its final result. Okay. So with that, we'll define the interpreter. The interpreter itself is very simple, right? So it's the interpreter that you will naturally define. The one thing that we want, we are going to place a constraint on is to make our interpreter a total function. So what are total functions? Total functions always give you a value, right? When you, for every argument, a result is defined. So that's the input. That's the definition. And what does it mean to have a totality for evaluation? You give it any expression, it will always evaluate to some value. That's the idea here. So what are we doing here? So evaluator, the interpreter, I call the function eval is a recursive function. It pattern matches on the given expression, right? If the value is, if the expression is a value, then the result is just V, right? So I have a value. So the value is just the result is just V. If the expression is E1 plus E2, first evaluate E1 to get a V1, then evaluate E2 to get a V2. And then you actually add them together to get the final result. And for division, it's similar, right? Evaluate E1 to get a V1, evaluate E2 to get a V2. And then V1 divided by V2 is the result of evaluating this expression. Right? These are recursive calls because they can be taken this, this E1 could itself be a more complex expression, which is like plus division also. So the type of this evaluator is it takes an expression and then returns you an integer. That's the result of interpreting this expression, interpreting evaluating this expression. Okay. That should be fairly obvious. So let's take a step, right? So let's see some examples. So what I'm doing here is I've written down plus dev val4 val2 val7. The way to read this is it's better to look at it in in fixed form. It's basically encoding four divided by two plus seven. Right? And the result is going to be nine. So if you run this, you're going to get nine. Oops. Let me clear the restart and clear output. And then let's run these things."
  },
  {
    "slide": "slides/scene_0011.png",
    "t_start": 2385.2714285714287,
    "t_end": 2623.7985714285714,
    "text": "So if you run this expression, you will get a you'll get a nine. Right? So this is two plus seven is nine. So the problem with this, right? So the thing that I that we really wanted is we want this function to be total in the sense that whatever expression I give you, I need an integer as a result, because that's the type that I've defined. But what happens when the I have a division where the denominator of the division is a zero. Right? And what happens in our evaluator if I divide the one by zero, I get an exception, I get a division by zero exception in OCaml, which is precisely what I have here. Right? And the point here is this is not a total function, right? If eval were a total function, whatever the input is, the input is this expression now, it should return me. It should return me what? It should return me an integer, but it is not returning me integer. It is not returning anything. Right? It is throwing an exception. This is hence this function is not a total function. Right? So I would say that because the specification says that it has to return an integer and this is this function is not running an integer. It is not a total function. So how can we avoid this? Right? I want to make it a total function. So let me stop here. So any questions so far on this on whatever I've said so far. Okay, so the point is to make sure that we don't throw an exception. Right? And we've seen a trick to do this, right? Instead of throwing an exception, we can encode. We can encode the result as a option type. Right? So rewrite evaluate evaluator function such that it takes an expression and returns you an inter option. The idea is that if the expression, in fact, add a division by zero, you'll get a none out. Otherwise, you'll get some value. Right? That's the idea. So return none for division by zero. Instead of throwing an exception, you'll return division none and that becomes total at the point. Right? Whatever expression that you can construct, you will get one of the values of this type. It might be a none or some, but the specification here fully captures what the function is doing. So it becomes total at the point. Let's write the evaluator. It looks much bigger now. Why is this case? So we pattern match on E, right? If it's value, then we return some V. Right? If it is a plus, then we first evaluate E1. When we call evaluate E1, it is possible that the result is none as well. Right? Which means that E1 had a division by zero exception, which is now returning none. So if E1 returns none, then this whole expression is also going to return none. Right? You had a division by zero exception. So return none. If E1 evaluates to some value V1, then go ahead and evaluate E2. And the same way, if E2 returns you none, which means evaluating E2 led to a division by zero exception, then you return none because the whole thing is now just a division by zero exception. And otherwise, if it returns V2, you have V1 and V2 now. So you make up V1 plus V2 and then wrap it in sum so that the type checker is satisfied. Right? So that's the plus. And for division, you do the same thing for E1. If E1 evaluates to none, then you return none because evaluating E1 itself led to a division by zero exception."
  },
  {
    "slide": "slides/scene_0012.png",
    "t_start": 2623.7985714285714,
    "t_end": 2862.325714285714,
    "text": "If we evaluate E2, and if that returns none, return none. If it returns V2, here is where we have to make a check. Right? So we have V1 and V2. If V2 is zero, V2 is the denominator. Right? V2 is what we obtained by evaluating E2. So if V2 is zero, then we are going to get a division by zero exception. Then we are going to say, okay, there is no result here. This corresponds to a division by zero case. So here we if V2 is non-zero, then we return sum. Yeah, you can use try catch. Yeah. So Yeah, so you can you can use that as well. But the type will be the same, right? It will be you'll have to convert that to this type. But yes, so, as I mentioned earlier, right, so exceptions are also some sort of an effect. Right? And we are going to try to write purely functional program. And then we are going to simulate the notion of exceptions and so on. So use the using an exception is sort of, I would say, let's not use exceptions here, because we are going to try to define monads for that. But you're right, we can use and you can use that right catch to handle this. Okay, so we've written this down, right? So this is nothing surprising here. And this works as expected, right? So if you do four divided by two plus seven, they haven't run this. So that's why you're not getting an optional value. So if you do this, then you get some nine. And if you evaluate one divided by zero, you get enough. So let me see how much time I think I can explain this. So there's a lot of repeated code here, right? So there is a lot of repeated code here in particular, if we evaluate an expression, if we evaluate it, then we if it is none, then we return on. Otherwise, we go and evaluate e two, if it is non return on, then some v two, the interesting thing happens here, right? All of this is sort of the same between plus and between division. Right, so we have a lot of common code here that we repeated. Right? The only interesting bit is for addition, this happens for division, this happens. So let's try to sort of extract the common parts into some function, right? That is what we will do. And the way we will do it is through two helper functions, right? I use the name return and bind, because I'm defining a monad, but you can, for the sake of this example, you could have called it anything, anything that you wanted. Right? So what I'm going to do is to say I'm going to define a function called return that is just going to wrap that given value in a some constructor and return it. So it is just a return is just an alias for some here. And I define this function bind, right? And it takes a takes an M, which is an optional value, which is the result of evaluating an expression. Right?"
  },
  {
    "slide": "slides/scene_0013.png",
    "t_start": 2862.325714285714,
    "t_end": 3100.8528571428574,
    "text": "It is the it is an expression which when evaluated gives you an optional value. If I if the optional value is none, then I return none. And what is F here? F is the thing that you do on the value if it is not none. Right? So I take a function F, which is what you do if this thing actually the M actually evaluates to some value. If it evaluates to a V, I'm going to apply F to V. Right? So you go back here, what I'm abstracting here. The match M with none to none and some V is the thing that you see here in this implementation is match one with some V1. Right? And F is the thing that you do here. Right? F is the thing that you do here with V1. What are you going to do? I'm sort of making that into the higher order function. And for the second one, this is the thing that I've abstracted. And this is going to be captured by F. So that is that is what I have here. Right? So so F is the function that you apply when the value actually evaluates to some value, it doesn't throw a division by zero. Right? Okay, so now given that we have these two functions, right, let us rewrite the interpreter to use this. Right. So I've rewritten it down here. So as I mentioned before, what we have to do for the value one. So what we have done is we evaluate it. It would return either. Or some returns done is none. If it returns some value, we then we do something else. Right. And that is precisely what we have here. For bind, we say evaluate even go ahead and evaluate even and we and we apply bind to it. The semantics is that if evaluating even returns none, then we are going to return none. If evaluating bind, sorry, evaluating even return some value, we then we are going to take this function. This whole thing is the function. Right. That's the rest of the computation in the case of plus. We're going to take this whole thing. And we are going to say, OK, this V that you get as a result of evaluating even. Just just buying that to be one and just do whatever the body says. What do we do in the body? We say, OK, now we evaluate it to. Right. And if it was going to evaluate to none, then we are going to return none. That is what I achieved with bind by applying bind to this term. If we would if it evaluates to none, then we get none back. If it evaluates to some value V, then that value gets bound to V2. Right. So I get V1 and V2. So this function goes all the way from here to here. Right. I get V2 here. So you get here only if both of the E1 and E2 evaluate to some value. Right. If if none of you want to need to actually have a division by zero exception, then you come here. You get both the values. And the last thing you do is return V1 plus V2. Again, return is forget the C semantics for return. We are not doing C now. Just put that away for the moment. Return here just says it wraps the thing in some constructor. So this whole thing now returns some V1 plus V2, which is what we wanted. It's just written in this weird syntax because I want to make a point."
  },
  {
    "slide": "slides/scene_0014.png",
    "t_start": 3100.8528571428574,
    "t_end": 3339.38,
    "text": "But that is what is happening here. So just the same thing that we had written earlier using return using these two functions now. Right. And the same for division. So evaluate E1. If that evaluates to none, then this whole thing, this bind, right, says that if it evaluates to none, this whole expression is none because the function itself goes from here all the way to the end. Goes from here to the end. Right. If it evaluates to some value, then the value gets bound to V1. And what do you do with that value? Go and evaluate E2. If that evaluates to a value, then bind that to V2. Now we have V1 and V2. Now go and do the check because we know V2 is the denominator. If V2 is zero, then the whole thing is none. Otherwise, the return V1, if you were to play V2. So all I've done is I just rewritten the previous examples here. This whole thing, this big thing. Down to the smaller expression. Yeah, again. So this is this is one way of writing it. I'm still not. I've still not made the point, but let me stop here because I've already taken a lot of time. You must have questions here. So I'll wait a few minutes so that you can ask questions on this. The thing to note here is the only the one thing to note here is this function. Right. This bind. The bind takes an M and an F. The M here is E1. Right. That's just the term that we have here. This F is this function that goes all the way to the end here. Right. So that's the weird bit. So it's like saying what should I do if this eval goes to a value is do all of this computation. Right. We sort of encapsulate that in a function. And similarly, the function F argument for the second bind goes all the way from here to here. Right. Yeah. Otherwise, I think I think the syntax might be a bit odd, but if you sort of expand the bind in place, you'll get the you'll just read if you replace every bind here. Right. With the definition here, you will get the previous implementation. Yeah. Yeah. Bind and you're right. So bind and here are very specific. Right. You're defining one sort of computation. This is a computation which will encode an optional value. So bind and return you're very right in saying that these binds are very specific to the computation that we are defined here. So it's not defined the same way everywhere. So we'll see different notions of return and bind. Each one will be a different monad. Okay. So yeah, if you have other questions, ping me on Slack. I'll be around and I can answer the questions. Okay. So I'll stop here. Thank you very much. I will see you next week."
  },
  {
    "slide": "slides/scene_0015.png",
    "t_start": 3339.38,
    "t_end": 3339.38,
    "text": ""
  }
]