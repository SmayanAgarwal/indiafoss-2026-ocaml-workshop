% What the hardware provides for virtualisation: the two classic
% privilege levels plus the VT-x root mode underneath, each level
% owning a layer of page tables. Used in M12-L03.
\documentclass[tikz,border=4pt]{standalone}
\usepackage{tikz}
\input{_m10-styles.texinput}
\begin{document}
\begin{tikzpicture}[m10, x=1mm, y=-1mm]
  \node[greybox, minimum width=62mm, minimum height=10mm,
        font=\sffamily\large, anchor=north west] (u) at (0,0)
        {Processes \quad ring 3};
  \node[safebox, minimum width=62mm, minimum height=10mm,
        font=\sffamily\large, anchor=north west] (k) at (0,12)
        {Kernel \quad ring 0};
  \node[warnbox, minimum width=62mm, minimum height=10mm,
        font=\sffamily\large, anchor=north west] (h) at (0,24)
        {Hypervisor \quad VT-x root mode};
  \node[note, anchor=west, font=\sffamily\large, text=labelgrey]
        at (65,5) {sees only what its page table maps};
  \node[note, anchor=west, font=\sffamily\large, text=labelgrey]
        at (65,17) {owns the page tables, one per process};
  \node[note, anchor=west, font=\sffamily\large, text=labelgrey]
        at (65,29) {owns the EPT, one per guest (added 2005)};
\end{tikzpicture}
\end{document}
