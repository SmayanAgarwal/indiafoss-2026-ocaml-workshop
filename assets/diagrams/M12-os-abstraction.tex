% What an OS is for: many applications above, many devices below,
% the OS as the abstraction layer between them. The two boundary
% notes name the two interfaces (stable syscalls up, one driver
% per device down). Used in M12-L01.
\documentclass[tikz,border=4pt]{standalone}
\usepackage{tikz}
\input{_m10-styles.texinput}
\begin{document}
\begin{tikzpicture}[m10, x=1mm, y=-1mm]
  % Applications
  \foreach \x in {0, 31, 62} {
    \node[greybox, minimum width=28mm, minimum height=11mm,
          font=\sffamily\large, anchor=north west] at (\x,0)
          {application};
  }
  \node[note, font=\huge, text=labelgrey] at (98.5,5.5) {$\cdots$};
  % Boundary: the stable interface applications target
  \node[note, font=\sffamily\large\itshape, text=labelgrey]
        at (53,17) {one stable interface (system calls)};
  % The OS band
  \draw[rounded corners=2.5pt, fill=stackfill, draw=stackedge,
        line width=0.7pt] (0,21) rectangle (106,45);
  \node[note, font=\sffamily\Large\bfseries, text=stackedge]
        at (53,27) {Operating system};
  \node[box, fill=white, draw=stackedge, font=\sffamily\large,
        minimum width=40mm, minimum height=9mm, anchor=north west]
        at (6,32) {drivers};
  \node[box, fill=white, draw=stackedge, font=\sffamily\large,
        minimum width=46mm, minimum height=9mm, anchor=north west]
        at (54,32) {resource management};
  % Boundary: one driver per device
  \node[note, font=\sffamily\large\itshape, text=labelgrey]
        at (53,50) {one driver per device};
  % Hardware band
  \draw[rounded corners=2.5pt, fill=labelgrey!12, draw=labelgrey!70,
        line width=0.7pt] (0,54) rectangle (106,76);
  \node[note, font=\sffamily\Large\bfseries, text=labelgrey]
        at (53,60) {Hardware};
  \foreach \x/\lbl in {4/CPUs, 25/memory, 46/NICs, 67/disks} {
    \node[box, fill=white, draw=labelgrey!70, font=\sffamily\large,
          text=labelgrey!40!black, minimum width=19mm,
          minimum height=9mm, anchor=north west] at (\x,65) {\lbl};
  }
  \node[note, font=\huge, text=labelgrey] at (96.5,70) {$\cdots$};
\end{tikzpicture}
\end{document}
