% Conventional stack: an OCaml application process above a separate
% privileged kernel band, on hardware. OCaml-flavoured remake of the
% talk's Java diagram (KC, 2026-06-12). Used in M12-L02.
\documentclass[tikz,border=4pt]{standalone}
\usepackage{tikz}
\input{_m10-styles.texinput}
\begin{document}
\begin{tikzpicture}[m10, x=1mm, y=-1mm]
  % Process container
  \draw[rounded corners=2.5pt, fill=secfill!55, draw=secedge,
        line width=0.7pt] (0,0) rectangle (96,46);
  \node[note, anchor=north, font=\large\bfseries,
        text=secedge!75!black] at (48,1) {Process};
  % App row
  \node[safebox, minimum width=42mm, minimum height=9mm,
        font=\sffamily\large, anchor=north west] at (4,9)
        {Application};
  \node[safebox, minimum width=42mm, minimum height=9mm,
        font=\sffamily\large, anchor=north west] at (50,9)
        {opam packages};
  % Runtime row
  \node[safebox, minimum width=88mm, minimum height=9mm,
        font=\sffamily\large, anchor=north west] at (4,21)
        {OCaml runtime (GC, Lwt)};
  % C libraries row
  \node[safebox, minimum width=27mm, minimum height=9mm,
        font=\sffamily\large, anchor=north west] at (4,33) {libc};
  \node[safebox, minimum width=27mm, minimum height=9mm,
        font=\sffamily\large, anchor=north west] at (34.5,33) {libssl};
  \node[safebox, minimum width=27mm, minimum height=9mm,
        font=\sffamily\large, anchor=north west] at (65,33) {libm};
  % Kernel band (separate, privileged)
  \draw[rounded corners=2.5pt, fill=stackfill, draw=stackedge,
        line width=0.7pt] (0,50) rectangle (130,62);
  \node[note, font=\large\bfseries, text=stackedge] at (65,56)
        {Kernel};
  % Hardware
  \draw[rounded corners=2.5pt, fill=labelgrey!12, draw=labelgrey!70,
        line width=0.7pt] (0,66) rectangle (130,78);
  \node[note, font=\large\bfseries, text=labelgrey] at (65,72)
        {Hardware};
\end{tikzpicture}
\end{document}
