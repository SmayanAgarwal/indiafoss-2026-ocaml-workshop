% Two MirageOS unikernels side by side on one hypervisor: each is
% application + Mirage runtime in a single image, isolated by the
% hypervisor, on hardware. Replaces the ASCII art in the MirageOS
% basics lecture.
\documentclass[tikz,border=4pt]{standalone}
\usepackage{tikz}
\input{_m10-styles.texinput}
\begin{document}
\begin{tikzpicture}[m10, x=1mm, y=-1mm]
  % Two unikernel images
  \foreach \x in {0, 56} {
    \draw[rounded corners=2.5pt, fill=secfill!50, draw=secedge,
          line width=0.7pt] (\x,0) rectangle (\x+50,30);
    \node[safebox, minimum width=44mm, minimum height=10mm,
          font=\sffamily\large, anchor=north west] at (\x+3,3)
          {application (OCaml)};
    \node[safebox, minimum width=44mm, minimum height=10mm,
          font=\sffamily\large, align=center, anchor=north west]
          at (\x+3,16) {Mirage runtime};
  }
  \node[note, anchor=west, font=\sffamily\large\itshape,
        text=secedge!75!black] at (109,15) {one unikernel};
  % Hypervisor band
  \draw[rounded corners=2.5pt, fill=stackfill, draw=stackedge,
        line width=0.7pt] (0,34) rectangle (106,46);
  \node[note, font=\sffamily\Large\bfseries, text=stackedge]
        at (53,40) {Hypervisor (KVM via Solo5)};
  % Hardware band
  \draw[rounded corners=2.5pt, fill=labelgrey!12, draw=labelgrey!70,
        line width=0.7pt] (0,50) rectangle (106,62);
  \node[note, font=\sffamily\Large\bfseries, text=labelgrey]
        at (53,56) {Hardware};
\end{tikzpicture}
\end{document}
