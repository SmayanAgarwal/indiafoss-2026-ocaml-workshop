{
  "name": "nptel-quiz-backend",
  "version": "1.0.0",
  "private": true,
  "description": "Cloudflare Worker that records anonymous quiz responses for the NPTEL OCaml course.",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "migrate": "wrangler d1 migrations apply nptel-quiz --remote"
  },
  "devDependencies": {
    "wrangler": "^3.90.0"
  }
}
